using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Talteen : MonoBehaviour
{
   //if (Input.GetKey(KeyCode.A))
        
            //myrb.AddForce(Vector2.left* speed, ForceMode2D.Impulse);
            //JumperAnim.SetBool("IsWalkingR", true);

        

//if (Input.GetKey(KeyCode.D))

    //myrb.AddForce(Vector2.right * speed, ForceMode2D.Impulse);
    //JumperAnim.SetBool("IsWalkingL", true);
     //if (!IsGrounded)
        
            //JumperAnim.SetBool("IsWalkingR", false);
        




//if (!IsGrounded)

    //JumperAnim.SetBool("IsWalkingL", false);


}
